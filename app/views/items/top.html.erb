<div class="top">
<div class="container">
	<div class="row">
		<div class="col-xs-1">
        </div>

    <div class="col-xs-10">
    <h2 class="clearfix"></h2>
      <div class="item" >
        <% @items.each do |item| %>
          <%= link_to carts_path(item_id: item.id), method: :post, data: {confirm: "カートに追加しますか？"} do %>
            <%= attachment_image_tag item, :image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg" %>
          <% end %>
        <% end %>
      </div>
    </div>

    	<div class="col-xs-1">
        </div>
  </div>
</div>
</div>
